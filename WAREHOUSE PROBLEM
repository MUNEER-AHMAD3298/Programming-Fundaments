
#include <iostream>
#include <climits>
using namespace std;
int main() {
   long long int sales[5][30]={0};
   long long int quantitysold[5][30]={0};
     int quantity;
     float saleamount;
     // Data from Manager of warehouse
     for(int i=0;i<5;i++)
     {
         cout<<"\t\t\t\t\tDATA FOR WAREHOUSE "<<i+1<<endl;
         for(int j=0;j<30;j++)
         {
             cout<<"Enter the quantity for day "<<j+1<<endl;
             cin>>quantity;
             quantitysold[i][j]=quantity;
             cout<<"Enter Sale amount for day "<<j+1<<endl;
             cin>>saleamount;
             sales[i][j]=saleamount;
         }
     }
     long long int total_month_sale[5]={0};
     long long int total_month_quantity[5]={0};
      for(int i=0;i<5;i++)
     {
         cout<<"\t\t\t\t\tFOR WAREHOUSE "<<i+1<<endl;
         for(int j=0;j<30;j++)
         {
         total_month_sale[i]=total_month_sale[i]+sales[i][j];
         total_month_quantity[i]=total_month_quantity[i]+quantitysold[i][j];
         
         }
cout<<"Total sales in one month "<<total_month_sale[i]<<endl;
cout<<"Total quantity  in one month "<<total_month_quantity[i]<<endl;
     }
     // Maximum sale of EAch warehouse
     long long int ans=INT_MIN;
     long long int ans1=INT_MIN;
    long  int index[5]={0};
    long long index1[5]={0};
     for(int i=0;i<5;i++)
     {
         cout<<"\t\t\t\tFor Warehouse "<<i+1<<endl;;
         for(int j=0;j<30;j++)
         {
            if(quantitysold[i][j]>ans) 
            {
                ans=quantitysold[i][j];
                index[i]=j;
            }
              if(sales[i][j]>ans1) 
            {
                ans1=sales[i][j];
                index1[i]=j;
            }
         }
         cout<<"Maximum  sold quantity of warehouse "<<i+1<<" is on DAte "<<index[i]+1<<endl;
          cout<<"Maximum sale amount of warehouse "<<i+1<<" is on date "<<index1[i]+1<<endl;
     }
     //to find which warehouse had maximum quantity sold 
     
     long long int ans2=INT_MIN;
     int index2=0;
     for(int i=0;i<5;i++)
     {
         ans2=index1[i];
         index2=i;
     }
     cout<<"Maximum Sale is of warehouse "<<index2+1<<endl;
     //FOR QUERY TO FIND WHICH WAREHOUSE HAD MAXMIMUM SALE ON SPECIFIC DATE
     int input=0;
     long long int ans3=INT_MIN;
     int index3=0;
     cout<<"Enter the date to find that maximum sale and to find  warehouse ";
     cin>>input;
     for(int i=0;i<5;i++)
     {
         
            if(quantitysold[i][input-1]>ans3) 
            {
                ans3=quantitysold[i][input-1];
                index3=i;
            }
             
     }
     cout<<"Maximum sale is "<<ans3<<" of warehouse "<<index3+1;
    return 0;
}
