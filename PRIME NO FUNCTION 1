
#include <iostream>
using namespace std;
int prime(int)
{
    int num=0;
    cout<<"Enter the number "<<endl;
    cin>>num;
    if(num<2)
    {
    
        return 0;
    }
    else if(num==2)
    {
        
        return 1;
    }
    else{
    for(int i=2;i<num;i++)
    {
     if(num%i==0)
     {
       
         return 0;
     }
    }
    
    return 1;
    }
    
}
int main() {
    int n;
  int prime(int n); 
cout<<prime(n)<<endl;
    
}
